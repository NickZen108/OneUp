#!/usr/bin/env sh
set -eu

if command -v gradle >/dev/null 2>&1; then
  exec gradle "$@"
fi

echo "Gradle is required to build OneUp Android. Install Gradle 8.14.4 or run the GitHub Actions workflow." >&2
exit 1
